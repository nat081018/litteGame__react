import React from "react";
import ReactDOM from "react-dom";
import "./funComponent.css";

const Greetings = props => {
    <p>  использование без класса {props.first} {props.last}<p>

};

{/* export default Greetings */}

